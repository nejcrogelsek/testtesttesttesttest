import Box from '@mui/material/Box'
import { FC } from 'react'

interface Props {}

const TemplateName: FC<Props> = (props: Props) => {
  return <Box className="template-name"></Box>
}

export default TemplateName
